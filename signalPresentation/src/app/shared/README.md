# shared/

Reusable, presentation-only building blocks with no feature-specific logic:
generic UI components (buttons, inputs, modals), shared pipes/directives,
and cross-cutting utilities/types used by more than one feature.

Nothing here yet — the login feature didn't need anything shared. Add
components/pipes/directives here only once a second feature needs them too,
to avoid premature abstraction.
